<project name="framework-web">

  <property file="build.properties"/>

  <target name="clean">
      <delete dir="${target.dir}"/>
      <delete dir="${exploded.dir}"/>
      <delete file="*.war"/>
  </target>

</project>